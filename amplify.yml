version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node --version
        - yarn install --frozen-lockfile
        - yarn ampx generate outputs --app-id d2abcyagvq3j4o --branch main --out-dir .
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
